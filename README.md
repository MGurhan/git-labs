# Git Lab

## Git Workflow:
```
# shows status of your code changes
git status

# stages your changes
git add <file name>

# adds your changes to a "commit"
git commit -m "xyz"

# pushes this commit from your local pc to the remote (online) github repo
git push
```